import { SYSTEM_SECTION, SYSTEM_STEPS } from '@/data/content';
export default function SystemSection(){return <section id="sistema" className="section-pad" style={{background:'var(--surface-base)',borderTop:'1px solid var(--border)',padding:'96px 0'}}><div className="container"><div className="header-grid" style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:48,marginBottom:80,alignItems:'end'}}><div><p className="t-label" style={{color:'var(--accent)',marginBottom:16}}>{SYSTEM_SECTION.eyebrow}</p><h2 style={{fontFamily:'var(--font-display)',fontSize:'clamp(32px,5vw,var(--text-3xl))',fontWeight:700,textTransform:'uppercase',letterSpacing:'var(--tracking-wider)',lineHeight:1,color:'var(--fg)',whiteSpace:'pre-line'}}>{SYSTEM_SECTION.title}</h2></div><p className="t-body" style={{color:'var(--fg2)'}}>{SYSTEM_SECTION.description}</p></div><div className="steps-grid" style={{display:'grid',gridTemplateColumns:'repeat(4,1fr)',borderTop:'1px solid var(--border)'}}>{SYSTEM_STEPS.map((step,index)=><div key={step.number} style={{padding:'40px 32px',borderRight:index<SYSTEM_STEPS.length-1?'1px solid var(--border)':'none'}}><div style={{fontFamily:'var(--font-display)',fontSize:'var(--text-5xl)',fontWeight:700,color:'var(--fg3)',lineHeight:1,marginBottom:32,letterSpacing:'var(--tracking-tight)'}}>{step.number}</div><div style={{width:32,height:2,background:'var(--accent)',marginBottom:24}}/><h3 className="t-h3" style={{color:'var(--fg)',marginBottom:16}}>{step.title}</h3><p className="t-body-sm" style={{color:'var(--fg2)'}}>{step.description}</p></div>)}</div></div></section>}
